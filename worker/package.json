{
  "name": "yjrl-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute yjrl-db --local --file=./migrations/0001_schema.sql && wrangler d1 execute yjrl-db --local --file=./migrations/0002_seed.sql",
    "db:migrate:remote": "wrangler d1 execute yjrl-db --remote --file=./migrations/0001_schema.sql && wrangler d1 execute yjrl-db --remote --file=./migrations/0002_seed.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
