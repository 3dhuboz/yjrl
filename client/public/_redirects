# Redirect API calls so stale bundles and direct /api requests do not receive the SPA.
/api/*  https://yjrl-api.steve-700.workers.dev/api/:splat  307

# SPA fallback: serve index.html for all other routes.
/*  /index.html  200
